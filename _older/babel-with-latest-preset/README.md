`babel-cli` should be installed globally

```sh
babel blah.babel.js --out-file blah.js --watch
```

```sh
node blah.js    & rem Or `nodemon --quiet blah.js`
```

or:

```sh
babel-node blah.babel.js
```
